version: '3.8'

services:
  gvia:
    build: .
    container_name: gvia
    ports:
      - "8818:8818"
    volumes:
      # 配置文件挂载：宿主机 ./config/ 目录挂载到容器 /app/config/
      # 确保程序能读取到配置文件
      - ./config:/app/config
    environment:
      PORT: "8818"
    restart: unless-stopped
